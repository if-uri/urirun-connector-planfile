# TODO

- [x] Publish `if-uri/urirun-connector-planfile`.
- [x] Add connector detail page to `connect.ifuri.com`.
- [ ] Add this connector to IFURI-016 full host-node Docker matrix with
      create/list/update task routes.
- [x] Add entry-point based discovery through the `urirun.bindings` connector
      discovery contract.
- [ ] Add broader flow examples using host/node execution and planfile queues.
- [ ] Publish route schemas and task status transition examples on the connector
      detail page.
